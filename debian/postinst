#!/bin/sh
# postinst script for Tasks
#
# see: dh_installdeb(1)

set -e

# summary of how this script can be called:
#        * <postinst> `configure' <most-recently-configured-version>
#        * <old-postinst> `abort-upgrade' <new version>
#        * <conflictor's-postinst> `abort-remove' `in-favour' <package>
#          <new-version>
#        * <postinst> `abort-remove'
#        * <deconfigured's-postinst> `abort-deconfigure' `in-favour'
#          <failed-install-package> <version> `removing'
#          <conflicting-package> <version>
# for details, see https://www.debian.org/doc/debian-policy/ or
# the debian-policy package


case "$1" in
    configure)
        if [ ! -f /usr/share/applications/Tasks.desktop ]; then
            ln -s /opt/Tasks/share/applications/Tasks.desktop /usr/share/applications/Tasks.desktop
        fi
        if [ ! -d ~/.config/autostart ]; then
            mkdir -p ~/.config/autostart
        fi
        if [ ! -f  ~/.config/autostart/Tasks.desktop ]; then
            chmod a+wr ~/.config/autostart
            ln -s /opt/Tasks/share/applications/Tasks.desktop ~/.config/autostart/Tasks.desktop
        fi
        if [ ! -f /usr/share/pixmaps/Tasks.png ]; then
            if [ ! -d /usr/share/pixmaps ]; then
                mkdir -p /usr/share/pixmaps
            fi
            ln -s /opt/Tasks/share/pixmaps/Tasks.png /usr/share/pixmaps/Tasks.png
        fi
        echo "/opt/Tasks/lib" > /etc/ld.so.conf.d/Tasks.conf
        echo "/opt/Tasks/bin" >> /etc/ld.so.conf.d/Tasks.conf
        export QT_VERSION_DIR=
        if [ -n "${QT_VERSION_DIR}" ]; then
            echo "/opt/qt${QT_VERSION_DIR}/lib" >> /etc/ld.so.conf.d/FaceRecognizer.conf
        fi
        ldconfig
    ;;

    abort-upgrade|abort-remove|abort-deconfigure)
        rm -fr ~/.config/autostart/Tasks.desktop
        rm -fr /usr/share/applications/Tasks.desktop
        rm -fr /etc/ld.so.conf.d/Tasks.conf
        rm -fr /usr/share/pixmaps/Tasks.png
    ;;

    *)
        echo "postinst called with unknown argument \`$1'" >&2
        exit 1
    ;;
esac

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0
